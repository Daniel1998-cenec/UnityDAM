using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyScript : MonoBehaviour
{
    //Hacer que el enemigo persiga al jugador, y que cuando lo toque, que el jugador muera(opcional, sino luego se hace en el PlayerScript)
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
